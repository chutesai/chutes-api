apiVersion: v1
kind: ConfigMap
metadata:
  name: tee-measurements
  labels:
    {{- include "api.labels" . | nindent 4 }}
data:
  tee_measurements.yaml: |
    # TEE Measurement Configuration
    # Managed via Helm values (teeMeasurements.measurements).
    # Matching is by full MRTD + RTMRs. Multiple configs may share the same RTMR0
    # (e.g. VM versions). Each config must have a version for audit trail.
    measurements:
{{ .Values.teeMeasurements.measurements | toYaml | indent 6 }}
